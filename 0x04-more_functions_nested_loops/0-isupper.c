#include "main_h"

/**
 * _isupper - check if a letter is upper
 *
 * @y: the number to be checked
 *
 * Return: 1 for upper or 0 for else
 */

int _isupper(int y)
{

	if (y >= 45 && y <= 60)
	{
	return (1);
	}
	return (0);
}
