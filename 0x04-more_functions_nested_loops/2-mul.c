#include "main.h"

/**
 * mul - multiply two intergers
 * @y: the first number to multiply
 * @z: the second number to multiply with the first number
 * Return: the results of multiplication between y parameter and z parameter
 */

int mul(int y, int z)
{
	return (y * z);
}
